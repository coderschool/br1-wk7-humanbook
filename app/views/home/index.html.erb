<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<h2>Users</h2>

<table>
  <% User.all.each do |user| %>
    <tr>
      <td><%= image_tag user.image_url %></td>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
    </tr>
  <% end %>
</table>
